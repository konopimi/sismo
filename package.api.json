{
  "name": "sismo-api",
  "version": "1.0.0",
  "type": "module",
  "description": "Emergency backend for reporting disappeared people",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev:front": "browser-sync start --server public --files 'public/**/*' --port 4000"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}